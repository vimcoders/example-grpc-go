services:
  balance:
    build:
      dockerfile: cmd/balance/Dockerfile
    image: ${REGISTRY_URL}/balance:${IMAGE_ID}
    container_name: balance  # 自定义容器名称
    restart: always
    networks:
      - kube  # 加入私有网络
    environment:
      MYSQL: ${MYSQL_DSN}
    depends_on:
      redis:
        condition: service_healthy  # 等待 redis 健康后再启动
      mysql:
        condition: service_healthy  # 等待 mysql 健康后再启动
      nats:
        condition: service_healthy  # 等待 nats 健康后再启动
    ports:
      - "26888:26888"
      - "36888:36888"
  proxy:
    build:
      dockerfile: cmd/proxy/Dockerfile
    image: ${REGISTRY_URL}/proxy:${IMAGE_ID}
    container_name: proxy  # 自定义容器名称
    restart: always
    networks:
      - kube  # 加入私有网络
    environment:
      MYSQL: ${MYSQL_DSN}
    depends_on:
      redis:
        condition: service_healthy  # 等待 redis 健康后再启动
      mysql:
        condition: service_healthy  # 等待 mysql 健康后再启动
      nats:
        condition: service_healthy  # 等待 nats 健康后再启动
  social:
    build:
      dockerfile: cmd/social/Dockerfile
    image: ${REGISTRY_URL}/social:${IMAGE_ID}
    container_name: social  # 自定义容器名称
    restart: always
    networks:
      - kube  # 加入私有网络
    environment:
      MYSQL: ${MYSQL_DSN}
    depends_on:
      redis:
        condition: service_healthy  # 等待 redis 健康后再启动
      mysql:
        condition: service_healthy  # 等待 mysql 健康后再启动
      nats:
        condition: service_healthy  # 等待 nats 健康后再启动
  chat:
    build:
      dockerfile: cmd/chat/Dockerfile
    image: ${REGISTRY_URL}/chat:${IMAGE_ID}
    container_name: chat  # 自定义容器名称
    restart: always
    networks:
      - kube  # 加入私有网络
    environment:
      MYSQL: ${MYSQL_DSN}
    depends_on:
      redis:
        condition: service_healthy  # 等待 redis 健康后再启动
      mysql:
        condition: service_healthy  # 等待 mysql 健康后再启动
      nats:
        condition: service_healthy  # 等待 nats 健康后再启动
  activity:
    build:
      dockerfile: cmd/activity/Dockerfile
    image: ${REGISTRY_URL}/activity:${IMAGE_ID}
    container_name: activity  # 自定义容器名称
    restart: always
    networks:
      - kube  # 加入私有网络
    environment:
      MYSQL: ${MYSQL_DSN}
    depends_on:
      redis:
        condition: service_healthy  # 等待 redis 健康后再启动
      mysql:
        condition: service_healthy  # 等待 mysql 健康后再启动
      nats:
        condition: service_healthy  # 等待 nats 健康后再启动
  item:
    build:
      dockerfile: cmd/item/Dockerfile
    image: ${REGISTRY_URL}/item:${IMAGE_ID}
    container_name: item  # 自定义容器名称
    restart: always
    networks:
      - kube  # 加入私有网络
    environment:
      MYSQL: ${MYSQL_DSN}
    depends_on:
      redis:
        condition: service_healthy  # 等待 redis 健康后再启动
      mysql:
        condition: service_healthy  # 等待 mysql 健康后再启动
      nats:
        condition: service_healthy  # 等待 nats 健康后再启动
  mail:
    build:
      dockerfile: cmd/mail/Dockerfile
    image: ${REGISTRY_URL}/mail:${IMAGE_ID}
    container_name: mail  # 自定义容器名称
    restart: always
    networks:
      - kube  # 加入私有网络
    environment:
      MYSQL: ${MYSQL_DSN}
    depends_on:
      redis:
        condition: service_healthy  # 等待 redis 健康后再启动
      mysql:
        condition: service_healthy  # 等待 mysql 健康后再启动
      nats:
        condition: service_healthy  # 等待 nats 健康后再启动
  mysql:
    image: mysql:latest
    restart: always
    networks:
      - kube  # 加入私有网络
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_ROOT_PASSWORD: 'root'
    ports:
      - "3306:3306"
    # 健康检查配置：判断 MySQL 是否可连接
    healthcheck:
      # 测试命令：使用 mysqladmin  ping 检查连接
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p$$MYSQL_ROOT_PASSWORD"]
      interval: 5s        # 每5秒检查一次
      timeout: 5s         # 单次检查超时时间
      retries: 10         # 最多重试10次（总计约50秒）
      start_period: 15s   # 启动后15秒内不检查（预留初始化时间）
  nats:
    image: nats:latest
    ports:
      - "4222:4222"
      - "8222:8222"
    healthcheck:
      test: ["CMD", "nats-server", "--check"]
      interval: 2s
      timeout: 2s
      retries: 15
      start_period: 5s
    restart: always
    networks:
      - kube
  redis:
    image: redis:latest  # 放弃${IMAGE_ID}，用确定能跑的版本
    restart: always
    networks:
      - kube
    healthcheck:
      # 修复2：健康检查改对（端口6379，用redis-cli更可靠）
      test: ["CMD", "redis-cli", "PING"]  # 不用nc，直接用Redis自带命令
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 15s 
    ports:
      - "6379:6379"    # HTTP 端口（Let's Encrypt 验证必需）

networks:
  kube:
    driver: bridge  # 使用桥接驱动，默认选项